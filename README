This project implements basic crud operation of an "inventory" application using spring boot, maven, jpa, and is deployed using docker.

#SIMPLE DOCKER BUILD
imageid <--- docker pull pishka1/some-repo:spring-boot-docker-intro-tag
docker run -d -p:8080:8080 imageid

go to - http://localhost:8080/swagger-ui.html#

